'use client';

// Backward compatibility shim redirecting to the new structured folder
export { MoveInChecklist } from './MoveInChecklist/index';
export type { MoveInChecklistProps } from './MoveInChecklist/types';
